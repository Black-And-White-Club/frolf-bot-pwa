// place files you want to import through the `$lib` alias in this folder.

// Re-export commonly-used components from their actual locations after refactor
export { default as Button } from './components/general/Button.svelte';
export { default as ScoreCard } from './components/score/ScoreCard.svelte';
