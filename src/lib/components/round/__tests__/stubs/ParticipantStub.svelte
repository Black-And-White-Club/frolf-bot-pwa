<script lang="ts">
	import type { Round } from '$lib/types/backend';

	// 'round' is exported for external reference only and should be a constant
	export const round: Round = {} as Round;
	// 'compact' is exported for external reference only and should be a constant
	export const compact: boolean = false;
</script>

<div data-testid="participant-stub">Participant Stub</div>
